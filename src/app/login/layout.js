export const metadata = {
	title: "Iniciar Sesión | Esfrar",
	description: "Login of esfrar",
};

export default function RootLayout({ children }) {
	return (
		<html lang='es'>
			<body>{children}</body>
		</html>
	);
}
