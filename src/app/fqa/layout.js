export const metadata = {
	title: "Preguntas frecuentes | Esfrar",
	description: "Frequently Asked Questions",
};

export default function RootLayout({ children }) {
	return (
		<html lang='es'>
			<body>{children}</body>
		</html>
	);
}
